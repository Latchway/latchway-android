<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <provider
            android:name="dev.latchway.okhttp.LatchwayContextProvider"
            android:authorities="${applicationId}.latchway-context"
            android:exported="false"
            android:initOrder="101" />
    </application>
</manifest>
